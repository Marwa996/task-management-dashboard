import { TaskAssigneeNamePipe } from './task-assignee-name-pipe';

describe('TaskAssigneeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TaskAssigneeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
