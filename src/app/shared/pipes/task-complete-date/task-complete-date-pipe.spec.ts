import { TaskCompleteDatePipe } from './task-complete-date-pipe';

describe('TaskCompleteDatePipe', () => {
  it('create an instance', () => {
    const pipe = new TaskCompleteDatePipe();
    expect(pipe).toBeTruthy();
  });
});
