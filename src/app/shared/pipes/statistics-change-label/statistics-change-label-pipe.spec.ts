import { StatisticsChangeLabelPipe } from './statistics-change-label-pipe';

describe('StatisticsChangeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new StatisticsChangeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
